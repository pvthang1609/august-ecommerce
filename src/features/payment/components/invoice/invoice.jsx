import React from 'react';
import PropTypes from 'prop-types';

Invoice.propTypes = {
    no: PropTypes.
};

function Invoice(props) {
    const { no, name, products, discount, shipping, } =
    return (
        <div>
            
        </div>
    );
}

export default Invoice;